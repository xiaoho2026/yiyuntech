# **四川义云科技有限公司官网 UI 规格**

**Version:** 3.1**Status:** MVP**依赖:** `00_Product.md` v3

---

> ## v3.1 变更摘要 —— Hero 重设计
>
> 1. **Hero 由「全宽纯排版」改回左文右图两栏**：桌面端 `.hero-grid` 左右分栏，移动端单列堆叠、文案居中（§11）。
> 2. **右侧视觉改为内联 SVG 等距「AI 平台」插画**：层叠椭圆平台 + 等距 AI 立方体 + 四张悬浮玻璃卡（对话/柱状图/人形/文档）+ 静态轨道环；撤销旧版抽象 AI Network 与 v3 过渡期的扁平城市剪影（§12）。
> 3. **左侧文案细化**：H1 下增加蓝青渐变强调条 `.hero-bar`；两个 CTA 统一带 `→`；三个信任点改用差异化语义图标（shield-check / user-round / target），禁止重复对勾（§11.1）。
> 4. **动效收敛**：仅四张玻璃卡轻柔错峰悬浮，轨道与平台静态，尊重 `prefers-reduced-motion`（§12.3、§27.3）。
>
> ## v3 变更摘要
>
> 1. **AI Product 改为 4 张等大卡片 + 产品官网截图**，撤销 v2 的「产品展厅（主推通栏 / 半栏并排 / 平台通栏）」层级结构。桌面端 4 列、平板 2 列、移动 1 列。
> 2. **产品卡结构更新**：角色标签 → 产品截图 → 产品名称 → 副标题 → 描述 → 外链 CTA。撤销逐个产品的「代理 / 合作产品」归属标签，归属由区块底部统一免责说明承载。
> 3. **撤销统计栏**：无对应 UI 规格（内容红线禁止无来源数据）。
> 4. **About 并入 Footer**：不再设独立 About Section，无对应 UI 规格。
> 5. 首页由 8 段调整为 **7 段**。
>
> ## v2 变更摘要
>
> 1. 首页由 9 段调整为 **8 段**：撤销 `AI Transition`，新增 `AI Product（产品展厅）` 为第二屏。
> 2. 新增 **Product Showcase 组件**（§14–§15），取代原「四张等大业务卡片」。
> 3. 新增**归属标签**与**免责说明**的样式规范（§15.3、§14.2）—— v2 最重要的合规约束。
> 4. AI 场景列表更新为 6 个环节（新增「决策」），每项增加「相关产品」标注（§17.1）。
> 5. 流程由 4 步扩展为 **5 步**（新增「选择」），过渡文案「一个产品不是终点」（§18.1）。
> 6. 明暗节奏更新为 8 段（§35），响应式行为更新（§29）。
>
> 设计目标的变化：v1 要「像一家正在认真做 AI 企业应用的科技公司」；
> v2 要「像一家手上已经有几个实际 AI 产品、正在寻找企业应用场景的公司」。
>
> ## v2.1 变更摘要
>
> 1. **中文优先**：Section Eyebrow 与视觉标签全部由英文改为中文（仅保留品牌名与 AI 等通用词）。
>    依据 `02_Content.md §13.2`。
> 2. **Digital Infrastructure 视觉重做**：纯英文标签列表 → **竖向递进阶梯**（中文 + Lucide 图标 + 连接线）。
> 3. **Product Block 角色行增加图标**，让四个产品的角色可被快速区分。
> 4. 新增 `§26.1 图标使用场景` 与 `§26.2 图标选择原则`。
>
> ## v2.2 变更摘要 —— 对标 Linear / Anthropic / Vercel 的细节优化
>
> **方法**：直接抓取三个标杆站的线上样式表提取真实 Token，不依赖印象。
>
> **实证提取到的基准值**
>
> | 来源 | 实测值 |
> |---|---|
> | Linear Header | `background:#fffc`（80% 白）+ `backdrop-filter:blur(32px)` + `border:#00000014`（8% 黑 alpha） |
> | Linear Button | `border-radius:5px`；圆角档位 6 / 8 / 10 / 14px |
> | Linear 动效 | `transition:.16s var(--ease-out-quad)` |
> | Linear 移动端 Hero | `font-size:38px`（@media max-width:640px） |
> | Anthropic | `border-radius:2–4px`；正文 15px；字号档 24 / 32 / 38 / 40px；暖白 `#faf9f5`；近黑 `#141413` |
>
> **一条重要的自我修正**：Linear 的 `38px` 是**移动端**覆盖值，不是桌面端 Hero。
> 因此**没有**据此下调桌面 H1（仍为 72px）。推断基准值时必须确认它所处的 media query。
>
> **据此落地的改动**
>
> 1. **边框改用 alpha**：`#E5E7EB` → `rgba(17,19,24,.10)`；深色侧 → `rgba(255,255,255,.12)`。
>    实色边框在深浅交替的区块上会「跳」，alpha 边框在任何背景上都稳定。
> 2. **圆角整体收紧 2px**：按钮 8→6，卡片/产品块 12→10，标签 6→5。
> 3. **动效加快**：250ms → **170ms**（Linear 实测 160ms），hover 位移 4px → 3px。
> 4. **移除卡片阴影**：标杆站几乎不用 box-shadow，靠边框与背景建立层次。
> 5. **中文排版修正**：H1 行高 1.08 → 1.12（两行会贴）；负字距 -0.04em → -0.02em。
>    中文方块字不适合强负字距，会挤在一起。
> 6. **补齐字号中间档位**：原 48px → 22px 之间无过渡，新增 30 / 38px 两档。
> 7. **移动端**：Section 间距 72 → 60px；Hero min-height 680 → 620px；≤420px 时 CTA 全宽。
> 8. **新增导航当前区块高亮**（scrollspy），补齐「我在哪」的状态反馈。
> 9. **Token 化**：字号、圆角、间距、动效全部收敛为 CSS 变量，消除散落的硬编码。
>
> ## v2.3 变更摘要 —— 删装饰视觉 + 全宽 Hero + 浅色主导配色
>
> **背景**：用户质疑「AI 网络视觉是否必要」「深色是否必要」。两个问题的答案都是「不必要」。
>
> ### 一、删除 Hero AI 网络（原 §12 整节作废）
>
> 判断依据：
>
> 1. **它不承载任何信息。** 不说明公司是做什么的、有什么产品、解决什么问题。
> 2. **v2 之后更没必要。** Hero 下方第二段就是四个真实产品，真正有说服力的视觉是产品本身。
> 3. **它是全页唯一的装饰性视觉**，其余七段全靠 Typography + 内容，唯独这里插装饰，不统一。
> 4. **§37 判断优先级**：AI 视觉感排第 7，装饰性效果排第 8。
> 5. 精神上属于 §3.3 要避免的「通用 AI 装饰」。
>
> 连带收益：`site.js` 删除 92 行（252 → 160 行），首屏不再执行视觉生成代码，
> 不再需要 `prefers-reduced-motion` 的动画分支。
>
> ### 二、Hero 改为全宽纯排版
>
> 删掉视觉后，H1 从 55% 左列改占满全宽（`.hero-inner{max-width:1080px}`），
> 单行呈现，冲击力比原来被挤在半栏里更强。
>
> ### 三、全站改为浅色主导，深色只保留 Footer
>
> 实测基准（抓线上样式表量化）：
>
> | 站点 | 深色占比 | 明暗反转 |
> |---|---|---|
> | Linear | 100%（单一面 `--color-bg-marketing:#010102`） | **0 次** |
> | Anthropic | 29.6% | 少 |
> | Stripe | ≈0% 纯黑，用彩色浅底分区（`#ffe0d1` `#ecedfe`） | 0 次 |
>
> **没有一家是「每段黑白交替」。** 分区靠浅色微调，不靠明暗反转。
>
> **深色必须退出的三个具体理由**（依据 `00_Product.md §16 原则四`：主要访问场景是
> 微信、企业家活动现场扫码、企业客户转发）：
>
> 1. **微信转发**：深色页面截图在会话里过于扎眼，与周围浅色内容冲突。
> 2. **现场投屏**：深色在投影上对比度表现差，后排基本看不清。
> 3. **长中文正文**：深色底阅读负担更重；传统行业客户易把深色与「不像正经公司」挂钩。
>
> Linear 能全站深色是因为用户是开发者/设计师；我们的用户是中小企业负责人，人群不同。
>
> **新节奏（相邻区块永不同色，0 次明暗反转）**
>
> ```text
> 01 Hero                #FFFFFF      + 底部 1px hairline
> 02 AI Product          #F5F6F8      中性浅灰（白色产品卡浮起）
> 03 AI Application      #FFFFFF
> 04 AI Implementation   #F5F6F8
> 05 Digital Infra       #EEF2FB      浅蓝调（服务「数字空间」语义）
> 06 About               #FFFFFF
> 07 Contact             #F5F6F8
> 08 Footer              #080A0F      全站唯一深色
> ```
>
> 深色只留在 Footer 的原因：不在阅读区，上述三个风险一个都不沾，且深色页脚是行业通用做法。
>
> ### 四、连带调整
>
> - 删除 `.eyebrow-dark`、`.btn-on-dark`、`.btn-ghost-dark`——已无深色内容区
> - AI Application 与 About 两段由深色改浅色，场景列表与流程样式同步转浅
> - 404 由深色改浅色，与主站一致
> - 新增三档极浅底色变量：`--surface / --bg-tint / --bg-tint-blue`

---

# **1. UI 定义**

本文件定义四川义云科技有限公司官网的：

- 视觉语言
- 页面结构
- 布局
- 色彩
- Typography
- 组件
- 动画
- 响应式行为
- 交互规则

本文件不重新定义公司业务、品牌定位或业务边界。

产品定位以 `00_Product.md` 为准。

---

# **2. UI 总体目标**

网站视觉定位：

**AI Native Enterprise**

要求同时体现：

**AI 的前沿感**

和：

**企业服务的可信度**

视觉关键词：

```text
Modern
AI Native
Minimal
Enterprise
Intelligent
Trustworthy
```

整体视觉不是传统科技公司官网，也不是 AI 创业公司的炫技 Landing Page。

目标效果：

**像一家正在认真做 AI 企业应用的科技公司。**

---

# **3. 设计原则**

## **3.1 极简**

页面内容必须有足够留白。

不通过增加装饰填充页面。

---

## **3.2 强 Typography**

网站视觉重心由：

**Typography + Layout + Abstract AI Visual**

构成。

而不是：

**图片 + 卡片 + 图标**

---

## **3.3 AI 感来自动态系统**

AI 视觉主要通过：

- Mesh
- Node
- Network
- Data Flow
- Gradient
- Abstract UI

实现。

禁止依赖：

- 机器人
- 芯片
- 地球
- 电路板
- 代码雨

---

## **3.4 企业感来自秩序**

企业可信度主要通过：

- 严谨网格
- 清晰层级
- 克制动画
- 高质量 Typography
- 大量留白
- 清晰 CTA

建立。

---

# **4. Layout System**

## **4.1 Container**

最大宽度：

```text
1280px
```

桌面：

```text
width: min(1280px, calc(100% - 64px))
margin: 0 auto
```

Tablet：

```text
width: calc(100% - 48px)
```

Mobile：

```text
width: calc(100% - 40px)
```

---

# **5. Breakpoints**

使用：

```text
Mobile:  < 768px
Tablet:  768px – 1199px
Desktop: ≥ 1200px
```

不针对具体设备型号编写特殊规则。

---

# **6. Spacing**

基础单位：

```text
4px
```

常用：

```text
8
12
16
20
24
32
40
48
64
80
96
120
```

Desktop Section：

```text
120px 0
```

Tablet：

```text
96px 0
```

Mobile：

```text
60px 0      ← v2.2：由 72px 收到 60px
```

> v2.2：390px 宽的手机上，上下各 72px 等于 144px 空白，占屏幕高度约 17%，
> 用户需要多滑一次才能看到下一个 Section。收到 60px 更紧凑。

---

# **7. Color System**

## **7.1 Light**

```text
Background
#F7F8FA

Surface
#FFFFFF

Foreground
#111318

Muted
#6B7280

Border
rgba(17,19,24,.10)      ← v2.2：由实色 #E5E7EB 改为 alpha
```

---

## **7.2 Dark**

```text
Background
#080A0F

Surface
#11141B

Foreground
#F7F8FA

Muted
#9CA3AF

Border
rgba(255,255,255,0.12)   ← v2.2：由 0.10 微提到 0.12
```

> v2.2：边框统一改用 alpha。实色边框在深浅交替的区块上会出现视觉跳变，
> alpha 边框在任何背景上都稳定。依据 Linear 实测 `border:#00000014` / `#ffffff14`。

---

# **8. AI Accent**

主要强调：

```text
AI Blue
#5B7CFF
```

辅助：

```text
AI Purple
#8B5CF6

AI Cyan
#22D3EE
```

渐变：

```text
linear-gradient(
  120deg,
  #5B7CFF,
  #8B5CF6,
  #22D3EE
)
```

使用范围严格限制。

只允许用于：

- AI Visual
- 小面积渐变
- Hover
- Active
- CTA emphasis
- Node
- Data Flow

禁止大面积纯色填充。

---

# **9. Typography**

## **9.1 Font**

中文：

**Noto Sans SC**

英文：

**Inter**

Fallback：

```text
"Noto Sans SC",
"Inter",
-apple-system,
BlinkMacSystemFont,
"Segoe UI",
sans-serif
```

---

## **9.2 Desktop**

Hero H1：

```text
72px
line-height: 1.12          ← v2.2：由 1.08 放开
font-weight: 700
letter-spacing: -0.02em    ← v2.2：由 -0.04em 放开
```

Section H2：

```text
48px
line-height: 1.26          ← v2.2：由 1.15 放开
font-weight: 700
letter-spacing: -0.012em   ← v2.2：由 -0.03em 放开
```

> **v2.2 中文排版修正（重要）**
>
> 原值照搬了英文站的做法，但中文是**方块字**，排版规律不同：
>
> - **行高**：英文有升部降部，1.08 不会撞行；中文每个字填满整个 em 框，
>   1.08 会让两行标题几乎贴在一起。放开到 1.12 / 1.26。
> - **字距**：英文强负字距（-0.04em）让字母更紧凑、更有设计感；
>   中文方块字本身间距均匀，强负字距只会让字**挤成一团**。
>   放开到 -0.02em / -0.012em。
>
> 这两条在英文站上是对的，搬到中文站上是错的。

H3：

```text
22px
line-height: 1.4
font-weight: 600
```

Body：

```text
17px
line-height: 1.8
font-weight: 400
```

Small：

```text
15px
line-height: 1.6
```

---

## **9.2.1 完整字号阶梯（v2.2）**

v2.1 的问题是 48px → 22px 之间**没有过渡档位**，导致过渡标题只能硬套 H2 或 H3。

补齐后：

| Token | Desktop | Tablet | Mobile | 用途 |
|---|---|---|---|---|
| `--fs-hero` | 72 | 52 | 38 | Hero H1 |
| `--fs-h2` | 48 | 38 | 30 | Section 标题 |
| `--fs-statement` | 38 | 30 | 26 | 大强调（先找到问题 / 不是为了 AI 而 AI） |
| `--fs-product-lg` | 30 | 26 | 22 | 主推与平台产品名 |
| `--fs-product` | 24 | 22 | 20 | 标准产品名 |
| `--fs-h3` | 22 | 20 | 19 | 流程步骤 / 场景标题 |
| `--fs-lead` | 19 | 19 | 18 | 强调正文 |
| `--fs-body` | 17 | 17 | 16 | 正文 |
| `--fs-sm` | 15 | 15 | 15 | 卡片描述 |
| `--fs-xs` | 13 | 13 | 13 | 标签 / 元信息 |
| `--fs-eyebrow` | 12 | 12 | 12 | Eyebrow |

全部以 CSS 变量实现，断点内只覆盖变量值，不重写具体规则。

---

## **9.3 Mobile**

Hero H1：

```text
38px                       ← v2.2：由 42px 下调（Linear 移动端实测 38px）
line-height: 1.18
letter-spacing: -0.02em
```

Section H2：

```text
30px                       ← v2.2：由 34px 下调
line-height: 1.28
```

H3：

```text
19px
```

Body：

```text
16px
line-height: 1.78
```

---

# **10. Header**

高度：

Desktop：

```text
72px
```

Mobile：

```text
64px
```

Header 默认：

```text
position: sticky
top: 0
z-index: 50
```

背景：

```text
rgba(255,255,255,0.72)
```

使用：

```text
backdrop-filter: blur(16px)
```

边框：

非常弱的 bottom border。

---

## **10.1 Logo**

第一版采用文字 Logo。

结构：

```text
义云科技
YIYUN TECHNOLOGY
```

主文字：

```text
18px
font-weight: 700
```

英文：

```text
9px
letter-spacing: 0.14em
```

不自行设计复杂 Logo 图形。

如果公司提供正式 Logo，优先替换。

---

## **10.2 Desktop Navigation**

导航：

```text
首页
AI产品
AI应用
关于我们
```

字体：

```text
14px
font-weight: 500
```

Hover：

foreground 从 muted → foreground。

Transition：

```text
200ms
```

---

## **10.3 Header CTA**

文字：

聊聊企业需求 →

高度：

```text
40px
```

Padding：

```text
16px
```

背景：

```text
#111318
```

文字：

```text
#FFFFFF
```

Radius：

```text
8px
```

Hover：

轻微提升亮度，不改变尺寸。

---

## **10.4 Mobile Navigation**

隐藏 Desktop Navigation。

显示：

```text
Logo
Menu
```

Menu 打开：

全屏导航。

项目：

```text
首页
AI产品
AI应用
关于我们
联系我们
```

底部：

聊聊企业需求 →

打开/关闭使用：

```text
opacity
transform
```

不使用复杂菜单动画。

---

# **11. Hero（v3.1：左文右图 + 等距 AI 平台插画）**

背景：

```text
linear-gradient(180deg, #FFFFFF 0%, #F5F8FF 100%)   ← 浅色微渐变
```

另叠加两个极低强度径向光斑 `.hero-glow`（装饰，`aria-hidden`）。

底部：

```text
border-bottom: 1px solid var(--border)
```

用于与紧邻的 Hero 下方区块（同为浅色）划界。

**不设 min-height。** 高度由 padding 与内容自然撑开：

```text
≥1200  padding: 100px 0 86px
≥1024  padding:  80px 0 72px
Mobile padding:  64px 0 56px
```

布局：

**桌面端左右两栏**，`.hero-grid` 为 CSS Grid；移动端自动堆叠为单列、文案居中。

```text
≥1200  grid-template-columns: 1fr 1fr;        gap: 60px
≥1024  grid-template-columns: 1.05fr .95fr;   gap: 40px
<1024  单列；.hero-copy 文本居中，.hero-visual 排在文案下方
```

- 左栏 `.hero-copy`：Eyebrow → H1 → 强调条 → Description → 双 CTA → 信任点。
- 右栏 `.hero-visual`：内联 SVG 等距插画（见 §12），`aria-hidden="true"`。
- 桌面端左对齐；移动端整体居中。

---

## **11.1 Hero Content**

Eyebrow：

YIYUN TECHNOLOGY

样式：

```text
12px
letter-spacing: 0.16em
text-transform: uppercase
```

颜色：

`#9CA3AF`

---

### **H1**

**让 AI 和数字技术真正进入企业**（"AI" 用 `.hero-hl` 主蓝 #3B6BFF 强调，允许 `<br>` 分两行）

颜色：

`var(--fg) #0B1121`（浅色底用深色字；其中 "AI" 为主蓝）

---

### **强调条 `.hero-bar`**

H1 正下方一段短横条，作为标题与正文的视觉分隔：

```text
width:46px  height:4px  border-radius:2px
background:linear-gradient(90deg, #3B6BFF, #22D3EE)
移动端 margin:auto 居中；≥1024 左对齐（margin-left:0）
```

---

### **Description**

面向中小企业的数字化与智能化服务，从选择合适的工具，到进入真实业务场景。

最大宽度：

```text
480px（移动端居中）
```

颜色：

`var(--muted) #5F6B86`

---

### **CTA**

Primary：

探索 AI 产品 →

Secondary：

聊聊企业需求 →

两个按钮均带 `→` 箭头（`.arrow`）。Primary 使用 AI Accent 渐变、落到 `#ai-product`；Secondary 使用透明背景 + border、落到 `#contact`（带 `data-contact-cta`）。

> 变更：Primary 从「探索 AI 应用」改为「探索 AI 产品」；v3.1 起两个 CTA 统一补箭头。≤420px 时两个按钮纵向堆叠、各自 100% 宽。

---

### **信任点 `.hero-trust`**

Description/CTA 下方一行三个信任点，每个为「线性图标 + 短文案」，图标 16px、主蓝；允许换行。

| 图标（Lucide 线性风） | 文案 | 语义 |
| --- | --- | --- |
| shield-check（盾牌+对勾） | 真实产品可体验 | 产品可上手 |
| user-round（人形） | 专业团队陪跑 | 有人服务 |
| target（同心圆靶心） | 从工具到落地 | 重结果 |

三个点必须使用**彼此差异化、且与文案语义匹配**的图标，禁止三个重复对勾或语义不符的通用图标。

---

# **12. Hero 右侧插画（v3.1：内联 SVG 等距 AI 平台）**

Hero 右栏不使用位图，采用**纯内联 SVG** 手绘等距（isometric）插画，与全站 Lucide 内联 SVG 体系一致、零依赖、可无损缩放。文件内对应 `svg.hero-illus`，`viewBox="0 0 560 430"`，`width:100%; max-width:560px`（≥1200 可到 600px）。

> 为什么不用位图：避免 AI 生成位图把 "AI" 字样画错；SVG 颜色、动效、明暗可控，且不增加网络请求。

## **12.1 构成元素（由后向前）**

```text
1. 柔光晕        中心径向渐变光斑（#5B7CFF，opacity 0→.30）
2. 轨道环 ×2     横向椭圆（rx228/ry112 虚线环 + rx190/ry86 实线环），
                 环上点缀 3–4 个蓝/青小圆点；轨道与节点保持【静态】，不整环旋转
3. 落地投影      平台下方模糊椭圆阴影
4. 三层等距平台  由大到小三层椭圆台（圆柱侧壁 path + 椭圆顶面），
                 蓝→浅蓝渐变，顶面近白，呈现层叠"平台/底座"
5. AI 立方体     等距三面立方体（顶面/左面/右面三色渐变），
                 居中白色粗体 "AI" 字样，落在顶层平台上
6. 悬浮玻璃卡×4  对话气泡 / 柱状图 / 人形 / 文档，各配 Lucide 线性小图标，
                 白色近透明渐变 + 1px 浅蓝描边 + 柔和投影，分布在立方体四周
```

## **12.2 配色**

```text
主蓝 #3B6BFF / #5B7CFF，青 #22D3EE
平台渐变 #93B0FF→#5E80EE（侧壁）、#FFFFFF→#E3ECFF（顶面）
立方体 顶面 #A6BEFF→#5B7CFF，左面 #4D74F5→#3459E6，右面 #2F54E0→#1E36B0
玻璃卡 #FFFFFF(.97)→#EDF2FF(.92)，描边 #DCE6FF，图标主蓝
```

全部为品牌蓝/青同色系，不引入额外色相。

## **12.3 动效**

- 仅四张玻璃卡做轻柔上下悬浮：`@keyframes hfloat`，幅度 ±9px、周期 6s，四张用负 `animation-delay`（-1.6s/-3.1s/-4.6s）错峰。
- `transform-box:fill-box; transform-origin:center`，保证 SVG 子元素围绕自身浮动。
- 轨道环、平台、立方体**保持静态**（不做整环旋转，避免横向椭圆被转到竖向产生"翻滚"感）。
- `@media (prefers-reduced-motion: reduce)` 下关闭所有浮动。

## **12.4 约束**

- 纯装饰，整体 `aria-hidden="true"`，不承载任何信息与文案（"AI" 仅为图形字样）。
- 不出现客户数、数据、排名等需要来源的信息。
- 窄屏随 `.hero-visual` 等比缩放、居中，不产生横向滚动。

---

# **13. Section 02 — AI Product**

锚点：`#ai-product`

背景：

`#F0F4FF`（`--bg-tint`，中性浅蓝调）

这是 v3 首页的第二屏。

作用：

用 4 张等大产品卡片 + 产品官网截图，直接回答客户的问题：**你们有什么 AI 东西可以给我看？**

> v3 变更：撤销 v2 的「产品展厅（主推通栏 / 半栏并排 / 平台通栏）」层级结构，改为 **4 张等大卡片网格 + 产品截图**。

---

## **13.1 Header**

布局：左右两列（`section-head-row`），左侧标题，右侧链接。

左侧：

Eyebrow：

AI 产品

H2：

**连接优秀的 AI 产品，满足企业不同需求**

右侧链接：

查看全部产品 →（指向 `#ai-product`，Text CTA 样式）

---

## **13.2 视觉要求**

必须表现为**4 张等大卡片网格（Product Card Grid）**。

允许：

- 产品官网截图作为卡片主视觉
- 角色标签（决策助手 / 获客销售 / 内容生产 / 企业平台）作为卡片元信息
- 卡片 hover 轻微上浮 + 阴影

不允许：

- 通过尺寸差异建立主次层级（v2 的主推/半栏/平台通栏结构已撤销）
- 逐个产品贴「代理 / 合作产品」标签（归属由底部统一免责说明承载）
- 展示任何效果数据、客户数量、排名
- 使用产品 Logo 图片作为独立标识（产品名称以文字呈现）

---

# **14. Product Card Grid**

与 Section 02 同一区块，背景 `#F0F4FF`。

---

## **14.1 布局**

Desktop（≥ 1200px）：

```text
grid-template-columns: repeat(4, 1fr)
gap: 20px
```

Tablet（768px – 1199px）：

```text
grid-template-columns: repeat(2, 1fr)
gap: 20px
```

Laptop（1024px – 1199px）特殊收紧：

```text
grid-template-columns: repeat(4, 1fr)
gap: 16px
卡片 padding: 14px 14px 18px
产品描述字号: 13px
```

Mobile（< 768px）：

```text
1 列
顺序：NBBOSS → AI 外呼手机 → 零壹数字人 → 厉害猫 AI
```

---

## **14.2 区块免责说明**

产品网格下方统一放置一行小字。

样式：

```text
font-size: 13px
color: var(--muted)
margin-top: 24px
text-align: center
```

内容：

以上产品为义云科技代理或合作的第三方产品，产品能力与数据以各产品官方为准。

要求：

**不可省略，不可改为更小的字号，不可移到页面底部。**

---

# **15. Product Card**

## **15.1 基础样式**

```text
background: #FFFFFF
border: 1px solid var(--border)
border-radius: 14px（--radius）
padding: 16px 16px 22px
overflow: hidden
```

Hover：

```text
translateY(-4px)
box-shadow: 0 20px 44px -18px rgba(59,107,255,.18)
transition: 180ms ease
```

---

## **15.2 卡片结构**

每张产品卡固定结构，顺序不可调换：

```text
① 角色标签（product-tag）
② 产品截图（product-media）
③ 产品名称（h3）
④ 产品副标题（product-subtitle）
⑤ 一句话描述（product-desc）
⑥ 了解产品 →（product-link，外链，新标签页）
```

---

## **15.3 角色标签（product-tag）**

样式：

```text
font-size: 12px
font-weight: 500
color: var(--blue)
background: rgba(59,107,255,.08)
border-radius: 6px
padding: 4px 10px
display: inline-block
margin-bottom: 14px
```

四个产品的角色标签：

| 产品 | 角色标签 |
|---|---|
| NBBOSS | 决策助手 |
| AI 外呼手机 | 获客销售 |
| 零壹数字人 | 内容生产 |
| 厉害猫 AI | 企业平台 |

---

## **15.4 产品截图（product-media）**

样式：

```text
aspect-ratio: 16 / 10
border-radius: 10px（--radius-sm）
margin-bottom: 18px
overflow: hidden
background: #FFFFFF
```

图片：

```text
width: 100%
height: 100%
object-fit: cover
object-position: center
```

PNG 截图特殊处理：

```text
object-fit: contain
padding: 14px
background: #F7F9FC
```

图片来源：

| 产品 | 图片来源 |
|---|---|
| NBBOSS | 外链 CDN：`https://www.nbboss.ai/assets/hero-product-cloth-6yCGwoZk.png` |
| AI 外呼手机 | 外链 CDN：`https://fuma-ai.ok.kimi.link/assets/hero-phone.jpg` |
| 零壹数字人 | 本地：`assets/images/zeroone-homepage.png` |
| 厉害猫 AI | 本地：`assets/images/lihaimao-enterprise.png` |

---

## **15.5 文字元素**

产品名称（h3）：

```text
font-size: var(--fs-h3)（Desktop 22px / Mobile 19px）
font-weight: 700
```

产品副标题（product-subtitle）：

```text
font-size: 13px
color: var(--blue)
font-weight: 500
margin-top: 5px
```

一句话描述（product-desc）：

```text
font-size: var(--fs-sm)（14px）
color: var(--muted)
line-height: 1.7
margin-top: 10px
```

外链 CTA（product-link）：

```text
display: inline-flex
align-items: center
gap: 5px
font-size: var(--fs-sm)
font-weight: 500
color: var(--fg)
margin-top: 16px
```

Hover：color → var(--blue)，箭头右移 3px。

必须：

```text
target="_blank"
rel="noopener noreferrer"
```

---

## **15.6 四个产品**

### **01 NBBOSS**

```text
角色标签：决策助手
产品名称：NBBOSS
副标题：AI 决策伙伴
描述：专为老板打造的 AI 决策伙伴，随时掌握业务数据和经营洞察。
链接：https://www.nbboss.ai/
```

### **02 AI 外呼手机**

```text
角色标签：获客销售
产品名称：AI 外呼手机
副标题：智能外呼 · 高效触达
描述：AI 自动外呼，智能筛选意向客户，提升销售效率与转化率。
链接：https://fuma-ai.ok.kimi.link/
合作产品：福马 AI
```

### **03 零壹数字人**

```text
角色标签：内容生产
产品名称：零壹数字人
副标题：数字人短视频 · 智能创作
描述：AI 数字人一键生成短视频，让内容创作更简单、更高效。
链接：https://agent.zeroone.chat/
```

### **04 厉害猫 AI**

```text
角色标签：企业平台
产品名称：厉害猫 AI
副标题：企业级 AI 应用平台
描述：集 AI 工作流、知识库、智能体于一体，助力企业全面智能化升级。
链接：https://www.lihaimao.com/
```

---

# **16. Section 03 — AI Application**

锚点：`#ai-application`

背景：

`#080A0F`

这是整个首页第二个视觉重点。

---

## **16.1 Layout**

Desktop：

```text
45% / 55%
```

左侧 sticky。

右侧环节列表。

---

## **16.2 Left Content**

Eyebrow：

AI 应用

H2：

**AI 可以进入企业哪些环节？**

正文：

不从技术开始，而从企业真正遇到的问题开始。

大型强调：

**先找到问题，** **再决定 AI。**

---

# **17. AI Scenario Rows**

6 个。

顺序：

```text
决策
获客
销售
内容
办公
管理
```

Row 不采用传统大卡片。

采用：

```text
border-bottom
```

的列表式设计。

每个：

```text
padding: 24px 0
```

结构：

```text
01
获客
如何降低获客成本？
相关产品：AI 外呼
 →
```

---

## **17.1 相关产品标注**

每个环节下方标注可对应的产品。

样式：

```text
font-size: 13px
color: #9CA3AF
margin-top: 8px
```

作用：

把抽象环节落到具体可用的工具上，直接回答「这个环节你们有什么东西」。

规则：

- 只标注**确实可对应**的产品
- 没有对应产品的环节，**不显示该行**
- 不得为了填满而强行关联

---

## **17.2 Hover**

Hover 当前项：

- 标题向右移动 4px
- Arrow opacity 0 → 1
- AI Gradient line 出现

不要改变整个列表高度。

---

# **18. Section 04 — AI Implementation**

锚点：`#ai-implementation`

背景：

`#FFFFFF`

---

## **18.1 过渡文案（流程之前）**

标题（H2）：

**一个产品不是终点**

正文：

企业真正需要的，不只是购买一个 AI 工具。

更重要的是找到适合自己的工具，并让它进入真实的工作流程。

从产品体验、团队培训，到具体业务场景的应用，我们希望陪企业把 AI 从「知道」变成「用起来」。

样式：

正文最大宽度 `720px`，与 H2 间距 `24px`。

过渡文案与下方流程标题之间的间距：

```text
72px（Desktop）/ 48px（Mobile）
```

---

## **18.2 流程标题与副标题**

Eyebrow：

服务方式

标题（H2）：

**从一个问题开始**

副标题：

技术只是手段，真正重要的是解决企业的问题。

---

## **18.3 Process**

Desktop：

**五列**。

```text
01      02      03      04      05

了解    判断    选择    尝试    落地
```

每项之间使用极细连接线。

Tablet：

```text
2 列（第 5 项单独占一行）
```

Mobile：

```text
单列纵向
```

---

## **18.4 Content**

### **01**

**了解**

了解企业当前的业务、团队和实际问题。

### **02**

**判断**

判断哪些问题适合使用 AI，哪些问题并不需要 AI。

### **03**

**选择**

在已合作的产品中选择合适的工具，或组合多个工具。

### **04**

**尝试**

在小范围内完成配置、培训与验证。

### **05**

**落地**

验证有效后，再进入实际业务流程并持续优化。

---

## **18.5 服务形式**

流程下方一行轻量说明：

```text
产品体验 / 企业培训 / AI 工作坊 / 场景落地 / 持续优化
```

使用与 Contact 入口标签相同的 tag 样式，居中排列。

不要做成卡片。

---

底部：

**不是为了 AI 而 AI。**

使用较大字号。

---

# **19. Section 05 — Digital Infrastructure**

锚点：`#infrastructure`

背景：

`#F7F8FA`

Desktop：

```text
50% / 50%
```

左：

Eyebrow：

企业数字化

标题（H2）：

**从物理空间，到数字空间**

正文：

我们并不是从 AI 开始。

长期以来，我们一直参与企业办公空间、弱电工程和智慧楼宇相关业务。

今天，我们正在把这种“让企业更智能”的能力，从办公空间进一步延伸到企业经营和工作方式本身。

---

## **19.1 Visual — 递进阶梯（Ladder）**

右侧使用**竖向递进阶梯**，表达「从物理空间到数字空间」的层层推进。

> v2.1 变更：原设计为纯英文标签 `BUILDING / SPACE / DEVICE / DATA / AI`。
> 目标用户是中小企业负责人，英文标签对他们没有沟通价值，已全部改为**中文 + Lucide 图标**。
> 依据 `02_Content.md §13.2 语言规则：中文优先`。

---

### **结构**

每一项固定结构：

```text
① 图标节点（40 × 40，圆角 10）
② 中文标签（17px / 500）
③ 中文补充说明（13px / muted）
④ 编号（13px / muted，右对齐）
```

五项内容：

| 编号 | 标签 | 补充说明 | Lucide 图标 |
|---|---|---|---|
| 01 | 楼宇 | 建筑与园区 | `building` |
| 02 | 空间 | 办公与经营场所 | `grid-2x2` |
| 03 | 设备 | 弱电与终端设备 | `monitor` |
| 04 | 数据 | 采集与联通 | `database` |
| 05 | 智能 | AI 进入实际业务 | `sparkle` |

---

### **样式**

容器：

```text
background: #FFFFFF
border: 1px solid #E5E7EB
border-radius: 12px
padding: 28px（Mobile）/ 40px（≥ 768px）
```

图标节点：

```text
width / height: 40px
border-radius: 10px
border: 1px solid #E5E7EB
background: #F7F8FA
color: #6B7280
icon: 18px
```

节点之间的连接线：

```text
width: 1px
height: 20px
background: #E5E7EB
```

终点（05 智能）强调：

```text
节点 border: rgba(91,124,255,.5)
节点 background: rgba(91,124,255,.08)
图标与标签 color: #5B7CFF
```

强调只允许用在这一个节点上，不做大面积填充。

---

### **要求**

- 使用 `<ol>` + `<li>`，因为它是**有序的递进关系**，语义上就是列表
- 每层只显示一行标签 + 一行补充说明，不得堆叠更多文字
- 这些标签只服务视觉表达，不承担新的业务承诺
- 禁止使用芯片、机器人、电路板等意象（遵守 §3.3）

---

# **20. About（并入 Footer）**

v3 起不再设独立 About Section。

关于我们的文案（不超过三句）放入 **Footer 品牌介绍区**（`footer-brand`），锚点 `#about` 指向 Footer 网格容器。

转型叙事（「并不是从 AI 开始」）由 Section 05 Digital Infrastructure 正文承载。

---

# **21. Section 06 — Contact**

锚点：`#contact`

背景：

`#FFFFFF`

高度：

Desktop：

```text
500px
```

Mobile：

```text
420px
```

居中布局。

Eyebrow：

先聊问题

H2：

**有一个企业问题，想试试 AI？**

Description：

不需要先想清楚解决方案，从你正在遇到的问题开始。

CTA：

**和我们聊聊 →**

---

## **21.1 Contact Topics**

CTA 上方可以使用三个轻量标签：

```text
AI 产品与选型
企业培训与活动
AI 应用落地
```

不要做成三个大型卡片。

---

# **22. Section 08 — Footer**

背景：

`#080A0F`

颜色：

白 / 灰。

结构：

Desktop：

```text
Brand       业务方向       关于
```

Mobile：

垂直排列。

Brand：

义云科技

YIYUN TECHNOLOGY

让 AI 和数字技术真正进入企业。

业务：

```text
AI 产品与选型
AI 应用落地
企业 AI 培训与活动
智慧楼宇与智能办公
```

关于：

```text
关于义云
联系我们
```

底部：

© 2026 四川义云科技有限公司

备案信息使用真实数据。

---

# **23. CTA Component**

全站只允许三种 CTA 类型。

## **Primary**

深色或 AI Accent。

用于：

**主要转化动作**

---

## **Secondary**

透明背景 + Border。

用于：

**次级动作**

---

## **Text CTA**

文字 + Arrow。

例如：

了解更多 →

用于：

**低优先级导航。**

---

## **External CTA**

v2 新增。

指向第三方产品官网时使用（Product Block 的「了解产品 →」）。

必须：

```text
target="_blank"
rel="noopener noreferrer"
```

必须配合外链图标（arrow-up-right，Lucide），让用户知道将离开本站。

样式：

与 Text CTA 一致，但箭头使用外链图标而非右箭头。

无障碍：

必须包含可供屏幕阅读器识别的提示文本，例如：

```html
<span class="sr-only">（在新标签页打开）</span>
```

---

# **24. Button 规则**

高度：

```text
40px / 44px / 48px
```

根据层级使用。

Radius：

```text
6px      ← v2.2：由 8px 收紧
```

> 圆角档位（v2.2 统一收敛为 CSS 变量，对标 Linear 5–10px / Anthropic 2–4px）：
>
> ```text
> --radius-lg  14px   大容器
> --radius     10px   卡片 / Product Block
> --radius-sm   6px   按钮
> --radius-xs   5px   标签 / 归属标签
> ```

禁止：

- 胶囊形按钮
- 巨大按钮
- 过度圆角
- 多彩按钮

---

# **25. Card 规则**

卡片不是网站主要视觉元素。

只在以下场景使用：

- AI Product Card Grid（§15 Product Card）
- 特定 AI 场景

禁止整个网站：

Section → Card → Card → Card → Card

连续堆叠。

页面必须通过：

**Typography + Grid + List + Space**

建立节奏。

---

# **26. Icon**

统一：

**Lucide Icons**

Stroke：

```text
1.5px – 2px
```

颜色继承文本。

禁止：

- Emoji
- 彩色 Icon
- 3D Icon
- 混合 Icon Library

---

## **26.1 图标使用场景**

v2.1 新增；v3 更新。

图标只在**能降低理解成本**的位置使用，不做装饰。

| 位置 | 尺寸 | 用途 |
|---|---|---|
| AI Implementation 流程步骤（step-ico） | 20px（容器 40px） | 让「了解 → 判断 → 选择 → 尝试 → 落地」五步语义可被快速区分 |
| AI Application 场景项 | 22px（容器 44px） | 让六个企业环节可被快速识别 |
| Digital Infrastructure 场景卡 | — | v3 改用场景图片，不再使用阶梯节点图标 |

流程步骤五步图标（Lucide 线性风格，24×24，stroke-width 2）：

| 步骤 | 图标 | 语义 |
|---|---|---|
| 01 了解 | messages-square（双向对话气泡） | 沟通、倾听、调研企业业务与问题 |
| 02 判断 | scale（天平） | 权衡、评估哪些问题适合用 AI |
| 03 选择 | blocks（积木组合） | 挑选并组合多个产品 / 工具 |
| 04 尝试 | refresh-cw（循环箭头） | 小范围试点、快速迭代验证 |
| 05 落地 | flag（旗帜） | 验证有效后部署、进入实际业务 |

> v3 变更：v2.1 曾规定「流程步骤不使用图标」，v3 页面为每步配置语义化图标。图标必须与该步骤文案一一对应，禁止使用语义不符的通用图标（如用时钟表示判断、用星星表示选择）。

不使用图标的位置：

- Hero 右侧等距插画（整幅为手绘内联 SVG，其中玻璃卡小图标随插画统一绘制，不走通用图标位规则）
- 纯文字标签行（如服务形式 tags）

---

## **26.2 图标选择原则**

- 一个图标只表达**一个具体事物**，不表达抽象概念
- 优先选择**实物或常见界面符号**（楼、显示器、数据库、电话、摄影机）
- 不使用需要文化背景才能理解的隐喻
- 不使用芯片、机器人、电路板、地球等意象（遵守 §3.3）

---

## **26.3 与语言规则的关系**

图标不能完全替代文字。

中文标签 + 图标同时使用，而不是「只留图标」。

原因：目标用户是中小企业负责人，纯图标对他们同样存在理解成本。

---

# **27. Motion System**

动画原则：

**Slow / Subtle / Purposeful**

动画不是视觉主角。

---

## **27.1 Reveal**

```text
opacity: 0 → 1
translateY: 20px → 0
duration: 600ms
ease: easeOut
```

Stagger：

```text
80ms
```

---

## **27.2 Card Hover**

```text
translateY(-3px)
duration: 170ms
ease: cubic-bezier(.25,.46,.45,.94)
```

> v2.2：由 `4px / 250ms` 收紧为 `3px / 170ms`。
> Linear 实测 `transition:.16s var(--ease-out-quad)` —— 标杆站的微动效比直觉更快、位移更小。
> 慢而大的位移会显得「廉价」。

**v2.2 补充：不使用 box-shadow。**

层次靠**边框色 + 背景微变化**建立，不靠投影。

```text
hover: border-color: rgba(91,124,255,.45)
       background: #FCFCFE
```

---

## **27.3 Hero 插画动效（v3.1）**

仅四张悬浮玻璃卡上下轻浮动（见 §12.3）：

```text
hfloat：周期 6s，幅度 ±9px，四张错峰（delay -1.6/-3.1/-4.6s），无限循环
轨道环、平台、立方体保持静态；prefers-reduced-motion 下全部关闭
```

旧版 AI Network 的 Node 漂移（8–20s）与 Data Flow 光点（4–8s）已随视觉一并删除，不再实现。

---

## **27.4 Scroll Trigger**

使用 Intersection Observer 或 Framer Motion viewport。

默认：

```text
once: true
```

元素首次进入 viewport 后执行。

不要反复播放。

---

# **28. Reduced Motion**

必须支持：

```text
@media (prefers-reduced-motion: reduce)
```

此状态：

- 禁止 Node 漂移
- 禁止 Data Flow
- 禁止复杂 Reveal
- 保留必要 hover transition 或直接取消

页面内容必须在无动画情况下完整可用。

---

# **29. Responsive Behavior**

## **Desktop**

Hero：

```text
55 / 45
```

AI Product（4 张等大卡片）：

```text
4 列等大
```

AI Application：

```text
45 / 55
```

Process：

```text
5 columns
```

Infrastructure：

```text
50 / 50
```

About：

```text
40 / 60
```

---

## **Tablet**

Hero：

```text
50 / 50
```

AI Product（4 张等大卡片）：

```text
2 列等大
```

AI Application：

上下布局。

Process：

```text
2 列（第 5 项单独一行）
```

Infrastructure：

上下布局。

About：

上下布局。

---

## **Mobile**

所有复杂 Grid：

```text
1 column
```

Hero：

```text
Content
↓
Visual
```

AI Product（4 张等大卡片）：

```text
1 列
NBBOSS → AI 外呼手机 → 零壹数字人 → 厉害猫 AI
```

Product Card 统一样式：

```text
padding: 16px 16px 22px
产品名：19px
```

AI Application：

```text
Title
↓
Scenario List
```

Process：

```text
Vertical
```

Infrastructure：

```text
Content
↓
Visual
```

About：

```text
Title
↓
Content
```

---

# **30. Mobile 特殊规则**

移动端不是简单缩小 Desktop。

必须重新调整：

- 内容顺序
- Typography
- spacing
- Visual height
- CTA 宽度

Hero CTA：

两个按钮可以：

```text
inline
```

如果宽度不足：

```text
stack
```

按钮最小点击区域：

```text
44px
```

---

## **30.1 v2.2 移动端细化**

### **Section 间距**

```text
60px 0
```

### **Hero 高度**

```text
min-height: 620px      ← 原 680px
```

### **CTA 宽度**

视口 ≤ 420px 时：

```text
Hero 两个按钮：纵向堆叠 + 各自 100% 宽
Contact 主 CTA：100% 宽，最大 320px
```

原因：320–400px 的机型上并排两个按钮会把文字挤到换行，点击区域也随之变小。

官网的主要访问场景是微信内置浏览器与手机浏览器（§16 原则四），这一档必须照顾。

### **导航当前位置**

桌面端导航启用**当前区块高亮**（scrollspy）：

```text
激活项：文字色 → foreground，底部 1.5px AI Blue 下划线
```

移动端菜单不需要（它是一次性跳转面板，打开即关闭）。

### **实现方式**

用 IntersectionObserver 的 `rootMargin: '-15% 0px -75% 0px'` 建立一条检测带，
而不是监听 scroll 事件 —— 避免滚动抖动与性能损耗。

---

# **31. Accessibility**

必须：

- semantic HTML
- 正确 H1 → H2 → H3 层级
- 所有按钮可键盘访问
- 所有交互元素有 focus state
- 文本与背景满足 WCAG AA
- 装饰性 SVG 设置 aria-hidden
- 图片必须有 alt
- 不通过颜色作为唯一信息

---

# **32. SEO UI 相关规则**

页面只能存在一个：

```text
H1
```

H1：

让 AI 和数字技术真正进入企业

Section 使用：

```text
H2
```

业务卡片使用：

```text
H3
```

不要为了视觉效果破坏语义层级。

---

# **33. 页面状态**

MVP 为静态企业官网。

不需要：

- Loading Screen
- Fake AI Chat
- AI Assistant
- Dashboard
- Login
- User Account

页面加载时直接进入 Hero。

禁止制作：

“正在初始化 AI……”

这种伪 AI Loading。

---

# **34. 404**

需要一个极简 404 页面。

内容：

**页面不存在**

你访问的内容可能已经移动或删除。

按钮：

返回首页 →

视觉与主站一致。

不需要复杂动画。

---

# **35. 空间与视觉节奏**

页面必须避免：

```text
Hero
↓
Card
↓
Card
↓
Card
↓
Card
↓
Footer
```

正确节奏（v3，7 段）：

```text
LIGHT（渐变白 → 浅蓝）
Hero

LIGHT BLUE（#F0F4FF）
AI Product（4 张等大卡片）

WHITE
AI Application

WARM WHITE（#FAF8F4）
AI Implementation

LIGHT BLUE（#E8EEFC）
Digital Infrastructure

DARK（#050816）
Contact

DARK（#050816）
Footer（About 并入此处）
```

整个页面形成：

**Light → Light Blue → White → Warm White → Light Blue → Dark → Dark**

> v3 变更：首页由 8 段调整为 7 段（撤销独立 About、撤销统计栏）；前 5 段为浅色主导，Contact 与 Footer 为深色。

---

# **36. UI 禁止事项**

AI Coding Agent 不得自行增加：

- 客户 Logo Wall
- 数据统计
- 用户数量
- 成功案例
- Testimonials
- 新闻
- Blog
- Pricing
- 产品商城
- 在线 AI Chat
- AI Copilot
- 复杂 Dashboard
- 虚构产品截图
- 虚构客户照片
- 机器人
- 地球
- 芯片
- 电路板
- 代码雨
- 大面积玻璃拟态
- 过度 3D
- 页面 Loading 动画
- 滚动进度条
- 鼠标跟随光效

除非未来产品需求明确增加。

---

# **37. UI 判断优先级**

如果实现过程中发生冲突，按照以下优先级：

```text
1. 内容可理解性
2. 信息层级
3. 企业可信度
4. 可访问性
5. 响应式体验
6. 页面性能
7. AI 视觉感
8. 装饰性效果
```

**AI 感不能凌驾于可用性和可信度。**

---

# **38. 最终视觉判断标准**

完成后，从一个完全不了解义云科技的企业用户视角检查。

用户打开首页：

**3 秒：**

应该感受到：

现代科技公司 / AI / 企业服务

**10 秒：**

应该理解：

这家公司在做企业数字化和 AI。

**30 秒：**

应该理解：

他们手上有一批可以真实使用的 AI 产品（覆盖决策、获客、内容、企业内部应用），
并且可以帮助企业把这些产品用起来，也做智慧楼宇和智能办公。

**浏览完整页：**

应该形成：

如果我的企业有一个 AI 或数字化问题，可以找他们聊聊。

如果无法达到上述效果，优先调整：

**Typography → Layout → Content hierarchy**

而不是继续添加动画和视觉效果。

---

# **39. UI 完成定义**

UI MVP 完成必须同时满足：

- Desktop 正常显示
- Tablet 正常显示
- Mobile 正常显示
- Hero 信息明确
- 四个 AI 产品与各自角色明确
- **每个产品都有归属标签，且区块有免责说明**
- AI 应用场景明确
- 服务流程明确（5 步）
- 智慧楼宇业务得到保留
- 联系入口明确
- 无虚构业务
- 无虚构数据
- 无虚构案例
- 无不必要功能
- 动画克制
- 页面性能良好
- Accessibility 基础要求满足
- SEO 语义结构正确

最终目标不是：

**做一个很酷的 AI 网站。**

而是：

**做一个看起来足够现代、可信，同时能够容纳义云科技未来 AI 业务发展的企业官网。**