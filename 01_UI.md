# **四川义云科技有限公司官网 UI 规格**

**Version:** 2.3**Status:** MVP**依赖:** `00_Product.md` v2

---

> ## v2 变更摘要
>
> 1. 首页由 9 段调整为 **8 段**：撤销 `AI Transition`，新增 `AI Product（产品展厅）` 为第二屏。
> 2. 新增 **Product Showcase 组件**（§14–§15），取代原「四张等大业务卡片」。
> 3. 新增**归属标签**与**免责说明**的样式规范（§15.3、§14.2）—— v2 最重要的合规约束。
> 4. AI 场景列表更新为 6 个环节（新增「决策」），每项增加「相关产品」标注（§17.1）。
> 5. 流程由 4 步扩展为 **5 步**（新增「选择」），过渡文案「一个产品不是终点」（§18.1）。
> 6. 明暗节奏更新为 8 段（§35），响应式行为更新（§29）。
>
> 设计目标的变化：v1 要「像一家正在认真做 AI 企业应用的科技公司」；
> v2 要「像一家手上已经有几个实际 AI 产品、正在寻找企业应用场景的公司」。
>
> ## v2.1 变更摘要
>
> 1. **中文优先**：Section Eyebrow 与视觉标签全部由英文改为中文（仅保留品牌名与 AI 等通用词）。
>    依据 `02_Content.md §13.2`。
> 2. **Digital Infrastructure 视觉重做**：纯英文标签列表 → **竖向递进阶梯**（中文 + Lucide 图标 + 连接线）。
> 3. **Product Block 角色行增加图标**，让四个产品的角色可被快速区分。
> 4. 新增 `§26.1 图标使用场景` 与 `§26.2 图标选择原则`。
>
> ## v2.2 变更摘要 —— 对标 Linear / Anthropic / Vercel 的细节优化
>
> **方法**：直接抓取三个标杆站的线上样式表提取真实 Token，不依赖印象。
>
> **实证提取到的基准值**
>
> | 来源 | 实测值 |
> |---|---|
> | Linear Header | `background:#fffc`（80% 白）+ `backdrop-filter:blur(32px)` + `border:#00000014`（8% 黑 alpha） |
> | Linear Button | `border-radius:5px`；圆角档位 6 / 8 / 10 / 14px |
> | Linear 动效 | `transition:.16s var(--ease-out-quad)` |
> | Linear 移动端 Hero | `font-size:38px`（@media max-width:640px） |
> | Anthropic | `border-radius:2–4px`；正文 15px；字号档 24 / 32 / 38 / 40px；暖白 `#faf9f5`；近黑 `#141413` |
>
> **一条重要的自我修正**：Linear 的 `38px` 是**移动端**覆盖值，不是桌面端 Hero。
> 因此**没有**据此下调桌面 H1（仍为 72px）。推断基准值时必须确认它所处的 media query。
>
> **据此落地的改动**
>
> 1. **边框改用 alpha**：`#E5E7EB` → `rgba(17,19,24,.10)`；深色侧 → `rgba(255,255,255,.12)`。
>    实色边框在深浅交替的区块上会「跳」，alpha 边框在任何背景上都稳定。
> 2. **圆角整体收紧 2px**：按钮 8→6，卡片/产品块 12→10，标签 6→5。
> 3. **动效加快**：250ms → **170ms**（Linear 实测 160ms），hover 位移 4px → 3px。
> 4. **移除卡片阴影**：标杆站几乎不用 box-shadow，靠边框与背景建立层次。
> 5. **中文排版修正**：H1 行高 1.08 → 1.12（两行会贴）；负字距 -0.04em → -0.02em。
>    中文方块字不适合强负字距，会挤在一起。
> 6. **补齐字号中间档位**：原 48px → 22px 之间无过渡，新增 30 / 38px 两档。
> 7. **移动端**：Section 间距 72 → 60px；Hero min-height 680 → 620px；≤420px 时 CTA 全宽。
> 8. **新增导航当前区块高亮**（scrollspy），补齐「我在哪」的状态反馈。
> 9. **Token 化**：字号、圆角、间距、动效全部收敛为 CSS 变量，消除散落的硬编码。
>
> ## v2.3 变更摘要 —— 删装饰视觉 + 全宽 Hero + 浅色主导配色
>
> **背景**：用户质疑「AI 网络视觉是否必要」「深色是否必要」。两个问题的答案都是「不必要」。
>
> ### 一、删除 Hero AI 网络（原 §12 整节作废）
>
> 判断依据：
>
> 1. **它不承载任何信息。** 不说明公司是做什么的、有什么产品、解决什么问题。
> 2. **v2 之后更没必要。** Hero 下方第二段就是四个真实产品，真正有说服力的视觉是产品本身。
> 3. **它是全页唯一的装饰性视觉**，其余七段全靠 Typography + 内容，唯独这里插装饰，不统一。
> 4. **§37 判断优先级**：AI 视觉感排第 7，装饰性效果排第 8。
> 5. 精神上属于 §3.3 要避免的「通用 AI 装饰」。
>
> 连带收益：`site.js` 删除 92 行（252 → 160 行），首屏不再执行视觉生成代码，
> 不再需要 `prefers-reduced-motion` 的动画分支。
>
> ### 二、Hero 改为全宽纯排版
>
> 删掉视觉后，H1 从 55% 左列改占满全宽（`.hero-inner{max-width:1080px}`），
> 单行呈现，冲击力比原来被挤在半栏里更强。
>
> ### 三、全站改为浅色主导，深色只保留 Footer
>
> 实测基准（抓线上样式表量化）：
>
> | 站点 | 深色占比 | 明暗反转 |
> |---|---|---|
> | Linear | 100%（单一面 `--color-bg-marketing:#010102`） | **0 次** |
> | Anthropic | 29.6% | 少 |
> | Stripe | ≈0% 纯黑，用彩色浅底分区（`#ffe0d1` `#ecedfe`） | 0 次 |
>
> **没有一家是「每段黑白交替」。** 分区靠浅色微调，不靠明暗反转。
>
> **深色必须退出的三个具体理由**（依据 `00_Product.md §16 原则四`：主要访问场景是
> 微信、企业家活动现场扫码、企业客户转发）：
>
> 1. **微信转发**：深色页面截图在会话里过于扎眼，与周围浅色内容冲突。
> 2. **现场投屏**：深色在投影上对比度表现差，后排基本看不清。
> 3. **长中文正文**：深色底阅读负担更重；传统行业客户易把深色与「不像正经公司」挂钩。
>
> Linear 能全站深色是因为用户是开发者/设计师；我们的用户是中小企业负责人，人群不同。
>
> **新节奏（相邻区块永不同色，0 次明暗反转）**
>
> ```text
> 01 Hero                #FFFFFF      + 底部 1px hairline
> 02 AI Product          #F5F6F8      中性浅灰（白色产品卡浮起）
> 03 AI Application      #FFFFFF
> 04 AI Implementation   #F5F6F8
> 05 Digital Infra       #EEF2FB      浅蓝调（服务「数字空间」语义）
> 06 About               #FFFFFF
> 07 Contact             #F5F6F8
> 08 Footer              #080A0F      全站唯一深色
> ```
>
> 深色只留在 Footer 的原因：不在阅读区，上述三个风险一个都不沾，且深色页脚是行业通用做法。
>
> ### 四、连带调整
>
> - 删除 `.eyebrow-dark`、`.btn-on-dark`、`.btn-ghost-dark`——已无深色内容区
> - AI Application 与 About 两段由深色改浅色，场景列表与流程样式同步转浅
> - 404 由深色改浅色，与主站一致
> - 新增三档极浅底色变量：`--surface / --bg-tint / --bg-tint-blue`

---

# **1. UI 定义**

本文件定义四川义云科技有限公司官网的：

- 视觉语言
- 页面结构
- 布局
- 色彩
- Typography
- 组件
- 动画
- 响应式行为
- 交互规则

本文件不重新定义公司业务、品牌定位或业务边界。

产品定位以 `00_Product.md` 为准。

---

# **2. UI 总体目标**

网站视觉定位：

**AI Native Enterprise**

要求同时体现：

**AI 的前沿感**

和：

**企业服务的可信度**

视觉关键词：

```text
Modern
AI Native
Minimal
Enterprise
Intelligent
Trustworthy
```

整体视觉不是传统科技公司官网，也不是 AI 创业公司的炫技 Landing Page。

目标效果：

**像一家正在认真做 AI 企业应用的科技公司。**

---

# **3. 设计原则**

## **3.1 极简**

页面内容必须有足够留白。

不通过增加装饰填充页面。

---

## **3.2 强 Typography**

网站视觉重心由：

**Typography + Layout + Abstract AI Visual**

构成。

而不是：

**图片 + 卡片 + 图标**

---

## **3.3 AI 感来自动态系统**

AI 视觉主要通过：

- Mesh
- Node
- Network
- Data Flow
- Gradient
- Abstract UI

实现。

禁止依赖：

- 机器人
- 芯片
- 地球
- 电路板
- 代码雨

---

## **3.4 企业感来自秩序**

企业可信度主要通过：

- 严谨网格
- 清晰层级
- 克制动画
- 高质量 Typography
- 大量留白
- 清晰 CTA

建立。

---

# **4. Layout System**

## **4.1 Container**

最大宽度：

```text
1280px
```

桌面：

```text
width: min(1280px, calc(100% - 64px))
margin: 0 auto
```

Tablet：

```text
width: calc(100% - 48px)
```

Mobile：

```text
width: calc(100% - 40px)
```

---

# **5. Breakpoints**

使用：

```text
Mobile:  < 768px
Tablet:  768px – 1199px
Desktop: ≥ 1200px
```

不针对具体设备型号编写特殊规则。

---

# **6. Spacing**

基础单位：

```text
4px
```

常用：

```text
8
12
16
20
24
32
40
48
64
80
96
120
```

Desktop Section：

```text
120px 0
```

Tablet：

```text
96px 0
```

Mobile：

```text
60px 0      ← v2.2：由 72px 收到 60px
```

> v2.2：390px 宽的手机上，上下各 72px 等于 144px 空白，占屏幕高度约 17%，
> 用户需要多滑一次才能看到下一个 Section。收到 60px 更紧凑。

---

# **7. Color System**

## **7.1 Light**

```text
Background
#F7F8FA

Surface
#FFFFFF

Foreground
#111318

Muted
#6B7280

Border
rgba(17,19,24,.10)      ← v2.2：由实色 #E5E7EB 改为 alpha
```

---

## **7.2 Dark**

```text
Background
#080A0F

Surface
#11141B

Foreground
#F7F8FA

Muted
#9CA3AF

Border
rgba(255,255,255,0.12)   ← v2.2：由 0.10 微提到 0.12
```

> v2.2：边框统一改用 alpha。实色边框在深浅交替的区块上会出现视觉跳变，
> alpha 边框在任何背景上都稳定。依据 Linear 实测 `border:#00000014` / `#ffffff14`。

---

# **8. AI Accent**

主要强调：

```text
AI Blue
#5B7CFF
```

辅助：

```text
AI Purple
#8B5CF6

AI Cyan
#22D3EE
```

渐变：

```text
linear-gradient(
  120deg,
  #5B7CFF,
  #8B5CF6,
  #22D3EE
)
```

使用范围严格限制。

只允许用于：

- AI Visual
- 小面积渐变
- Hover
- Active
- CTA emphasis
- Node
- Data Flow

禁止大面积纯色填充。

---

# **9. Typography**

## **9.1 Font**

中文：

**Noto Sans SC**

英文：

**Inter**

Fallback：

```text
"Noto Sans SC",
"Inter",
-apple-system,
BlinkMacSystemFont,
"Segoe UI",
sans-serif
```

---

## **9.2 Desktop**

Hero H1：

```text
72px
line-height: 1.12          ← v2.2：由 1.08 放开
font-weight: 700
letter-spacing: -0.02em    ← v2.2：由 -0.04em 放开
```

Section H2：

```text
48px
line-height: 1.26          ← v2.2：由 1.15 放开
font-weight: 700
letter-spacing: -0.012em   ← v2.2：由 -0.03em 放开
```

> **v2.2 中文排版修正（重要）**
>
> 原值照搬了英文站的做法，但中文是**方块字**，排版规律不同：
>
> - **行高**：英文有升部降部，1.08 不会撞行；中文每个字填满整个 em 框，
>   1.08 会让两行标题几乎贴在一起。放开到 1.12 / 1.26。
> - **字距**：英文强负字距（-0.04em）让字母更紧凑、更有设计感；
>   中文方块字本身间距均匀，强负字距只会让字**挤成一团**。
>   放开到 -0.02em / -0.012em。
>
> 这两条在英文站上是对的，搬到中文站上是错的。

H3：

```text
22px
line-height: 1.4
font-weight: 600
```

Body：

```text
17px
line-height: 1.8
font-weight: 400
```

Small：

```text
15px
line-height: 1.6
```

---

## **9.2.1 完整字号阶梯（v2.2）**

v2.1 的问题是 48px → 22px 之间**没有过渡档位**，导致过渡标题只能硬套 H2 或 H3。

补齐后：

| Token | Desktop | Tablet | Mobile | 用途 |
|---|---|---|---|---|
| `--fs-hero` | 72 | 52 | 38 | Hero H1 |
| `--fs-h2` | 48 | 38 | 30 | Section 标题 |
| `--fs-statement` | 38 | 30 | 26 | 大强调（先找到问题 / 不是为了 AI 而 AI） |
| `--fs-product-lg` | 30 | 26 | 22 | 主推与平台产品名 |
| `--fs-product` | 24 | 22 | 20 | 标准产品名 |
| `--fs-h3` | 22 | 20 | 19 | 流程步骤 / 场景标题 |
| `--fs-lead` | 19 | 19 | 18 | 强调正文 |
| `--fs-body` | 17 | 17 | 16 | 正文 |
| `--fs-sm` | 15 | 15 | 15 | 卡片描述 |
| `--fs-xs` | 13 | 13 | 13 | 标签 / 元信息 |
| `--fs-eyebrow` | 12 | 12 | 12 | Eyebrow |

全部以 CSS 变量实现，断点内只覆盖变量值，不重写具体规则。

---

## **9.3 Mobile**

Hero H1：

```text
38px                       ← v2.2：由 42px 下调（Linear 移动端实测 38px）
line-height: 1.18
letter-spacing: -0.02em
```

Section H2：

```text
30px                       ← v2.2：由 34px 下调
line-height: 1.28
```

H3：

```text
19px
```

Body：

```text
16px
line-height: 1.78
```

---

# **10. Header**

高度：

Desktop：

```text
72px
```

Mobile：

```text
64px
```

Header 默认：

```text
position: sticky
top: 0
z-index: 50
```

背景：

```text
rgba(255,255,255,0.72)
```

使用：

```text
backdrop-filter: blur(16px)
```

边框：

非常弱的 bottom border。

---

## **10.1 Logo**

第一版采用文字 Logo。

结构：

```text
义云科技
YIYUN TECHNOLOGY
```

主文字：

```text
18px
font-weight: 700
```

英文：

```text
9px
letter-spacing: 0.14em
```

不自行设计复杂 Logo 图形。

如果公司提供正式 Logo，优先替换。

---

## **10.2 Desktop Navigation**

导航：

```text
首页
AI产品
AI应用
关于我们
```

字体：

```text
14px
font-weight: 500
```

Hover：

foreground 从 muted → foreground。

Transition：

```text
200ms
```

---

## **10.3 Header CTA**

文字：

聊聊企业需求 →

高度：

```text
40px
```

Padding：

```text
16px
```

背景：

```text
#111318
```

文字：

```text
#FFFFFF
```

Radius：

```text
8px
```

Hover：

轻微提升亮度，不改变尺寸。

---

## **10.4 Mobile Navigation**

隐藏 Desktop Navigation。

显示：

```text
Logo
Menu
```

Menu 打开：

全屏导航。

项目：

```text
首页
AI产品
AI应用
关于我们
联系我们
```

底部：

聊聊企业需求 →

打开/关闭使用：

```text
opacity
transform
```

不使用复杂菜单动画。

---

# **11. Hero（v2.3 重写：全宽纯排版）**

背景：

```text
#FFFFFF      ← v2.3：由 #080A0F 改为浅色
```

底部：

```text
border-bottom: 1px solid rgba(17,19,24,.10)
```

用于与紧邻的 Hero 下方区块（同为浅色）划界。

**不设 min-height。** 高度由 padding 与内容自然撑开：

```text
Desktop  padding: 150px 0 130px
Tablet   padding: 120px 0 100px
Mobile   padding:  88px 0  72px
```

布局：

**全宽单列**，不再做 55% / 45% 分栏（右栏视觉已删除）。

```text
.hero-inner { max-width: 1080px }
.hero h1    { max-width: 16em }
```

H1 在 Desktop 单行呈现，冲击力比原来被挤在半栏里更强。

左对齐，不居中。

---

## **11.1 Hero Content**

Eyebrow：

YIYUN TECHNOLOGY

样式：

```text
12px
letter-spacing: 0.16em
text-transform: uppercase
```

颜色：

`#9CA3AF`

---

### **H1**

**让 AI 和数字技术真正进入企业**

宽度：

```text
max-width: 720px
```

颜色：

`#F7F8FA`

---

### **Description**

面向企业提供 AI 产品、应用与数字化服务，从选择合适的工具，到进入真实业务场景。

最大宽度：

```text
560px
```

颜色：

`#9CA3AF`

---

### **CTA**

Primary：

探索 AI 产品

Secondary：

聊聊企业需求 →

Primary 使用 AI Accent。

> 变更：Primary 从「探索 AI 应用」改为「探索 AI 产品」，落到 `#ai-product` 锚点。

Secondary 使用透明背景 + border。

---

# **12. Hero AI Visual**

Hero 不使用图片。

使用：

**SVG + CSS + Framer Motion**

生成抽象 AI Network。

核心结构：

```text
                AI
                 ●
              ╱  │  ╲
            ●    ●    ●
          ╱      │      ╲
        DATA   WORKFLOW  BUSINESS
```

实际视觉不得直接显示上述文字结构。

这是内部结构说明。

---

## **12.1 Node**

Node：

```text
6px – 10px
```

Outer Glow：

极低强度。

部分 Node 使用 AI Gradient。

---

## **12.2 Connection**

连接线：

```text
1px
opacity: 0.15 – 0.35
```

不要全部发光。

---

## **12.3 Data Flow**

沿连接线移动小型光点。

速度：

```text
4–8s
```

随机延迟。

---

## **12.4 Node Movement**

缓慢漂移：

```text
8–20s
```

Movement：

```text
±10px
```

禁止剧烈移动。

---

# **13. Section 02 — AI Product**

锚点：`#ai-product`

背景：

`#FFFFFF`

这是 v2 首页的**第二屏，也是新的视觉重心**。

作用：

用真实可展示的产品，直接回答客户的问题：**你们有什么 AI 东西可以给我看？**

> 变更：v1 的 `Section 02 — AI Transition` 已撤销，转型叙事并入 `Section 06 — About`。
> 首页第二屏从「讲方向」改为「上产品」。

---

## **13.1 Header**

Eyebrow：

AI 产品

H2：

**AI 正在进入企业的不同环节**

Description：

从经营决策，到获客、内容生产，再到企业内部应用，选择适合企业实际需求的 AI 工具。

Description 最大宽度：

```text
720px
```

H2 与 Description 之间的间距：

```text
20px
```

Description 与产品网格之间的间距：

```text
56px（Desktop）/ 40px（Mobile）
```

---

## **13.2 视觉要求**

必须表现为**产品展厅（Product Showcase）**。

允许：

- 通过**尺寸、留白、字重**建立主次层级
- 主推产品（NBBOSS）使用轻微 AI Accent 强调
- 归属标签作为统一元信息行

不允许：

- 四张等大卡片平铺（这是 v1 的做法，v2 明确禁止）
- 与 Section 03 的场景列表视觉雷同
- 使用产品 Logo 图片（第一版统一使用文字产品名）
- 展示任何效果数据、客户数量、排名

层级差异不得只靠颜色建立。

---

# **14. Showcase Grid**

与 Section 02 同一区块，背景 `#FFFFFF`。

> 变更：v1 的 `Section 03 — Business（我们正在做的四件事，2×2 等大卡片）` 已撤销。
> 四个方向被拆到不同层级：产品 → Section 02；应用落地 → Section 04；
> 培训 → Section 04 服务形式；智慧楼宇 → Section 05。

---

## **14.1 布局**

Desktop（≥ 1200px）与 Tablet（≥ 768px）：

```text
┌────────────────────────────────┐
│ 01 NBBOSS      AI 决策伙伴      │ ← 通栏，主推
├───────────────┬────────────────┤
│ 02 AI 外呼    │ 03 零一数字人   │ ← 并排
├───────────────┴────────────────┤
│ 04 厉害猫 AI  企业级 AI 应用平台 │ ← 通栏，平台位
└────────────────────────────────┘
```

Grid：

```text
grid-template-columns: 1fr 1fr
gap: 20px
```

主推项与平台项：

```text
grid-column: 1 / -1
```

Mobile（< 768px）：

```text
1 列
顺序：NBBOSS → AI 外呼 → 零一数字人 → 厉害猫 AI
```

---

## **14.2 免责说明**

产品网格下方统一放置一行小字。

样式：

```text
font-size: 13px
color: #6B7280
margin-top: 24px
```

内容：

以上产品为义云科技代理或合作的第三方产品，产品能力与数据以各产品官方为准。

要求：

**不可省略，不可改为更小的字号，不可移到页面底部。**

---

# **15. Product Block**

## **15.1 基础样式**

```text
background: #FFFFFF
border: 1px solid #E5E7EB
border-radius: 12px
```

Hover：

```text
translateY(-4px)
border-color: rgba(91,124,255,.5)
transition: 250ms ease
```

禁止：

- 使用产品 Logo 图片
- 使用第三方品牌色填充
- 展示任何效果数据、客户数量、排名
- 使用胶囊形按钮（遵守 §24）

---

## **15.2 尺寸分级**

| 类型 | 适用 | padding | 产品名 | min-height |
|---|---|---|---|---|
| Featured | NBBOSS | 40px | 32px | 220px |
| Standard | AI 外呼 / 零一数字人 | 32px | 24px | 220px |
| Platform | 厉害猫 AI | 40px | 32px | 200px |

Mobile 统一：

```text
padding: 24px
产品名：22px
```

说明：

层级差异必须由**尺寸、留白、字重**建立。

不得只靠颜色区分，否则色觉障碍用户无法感知层级（遵守 §31）。

---

## **15.3 结构**

每个 Product Block 固定结构，顺序不可调换：

```text
① 归属标签（代理 / 合作产品）
② 产品角色（13px，AI Blue）
③ 产品名称（Featured 32px / Standard 24px）
④ 一句话描述（15–17px，muted）
⑤ 了解产品 →（Text CTA，新标签页）
```

---

## **15.4 归属标签**

样式：

```text
font-size: 12px
color: #6B7280
border: 1px solid #E5E7EB
border-radius: 6px
padding: 2px 10px
```

文案：

```text
代理 / 合作产品
```

位置：

Product Block 顶部第一行，产品名称之前。

要求：

**每一个产品都必须显示，不可省略。**

这是 v2 最重要的合规约束（依据 `00_Product.md` §5.5）。

---

## **15.5 四个产品**

### **01 NBBOSS**

位置：第一位，通栏，主推。

```text
代理 / 合作产品
AI 决策伙伴
NBBOSS
面向企业经营者的 AI 决策与工作伙伴，把 AI 带进日常经营与管理。
了解产品 →
```

链接：

```text
https://www.nbboss.ai/
```

---

### **02 AI 外呼**

位置：第二位，半栏。

```text
代理 / 合作产品
AI 获客与销售
AI 外呼
让 AI 进入企业的客户触达与销售环节。
了解产品 →
```

补充：

产品形态为 **AI 外呼手机**（合作产品：福马 AI）。

「手机」只出现在补充说明中，**不得用于标题或主描述**，避免官网显得像在卖 AI 硬件。

链接：

```text
https://fuma-ai.ok.kimi.link/
```

---

### **03 零一数字人**

位置：第三位，半栏。

```text
代理 / 合作产品
AI 内容生产
零一数字人
用 AI 降低企业持续生产内容的门槛，覆盖短视频与数字人内容场景。
了解产品 →
```

链接：

```text
https://agent.zeroone.chat/
```

---

### **04 厉害猫 AI**

位置：第四位，通栏，平台位。

```text
代理 / 合作产品
企业级 AI 应用平台
厉害猫 AI
从 AI 工具到业务流程，作为企业 AI 应用的底层平台之一。
了解产品 →
```

说明：

不与前三个产品做成同等大小。

产品范围明显更宽，作为未来 **AI 应用落地** 业务的底层产品之一表达。

链接：

```text
https://www.lihaimao.com/
```

---

# **16. Section 03 — AI Application**

锚点：`#ai-application`

背景：

`#080A0F`

这是整个首页第二个视觉重点。

---

## **16.1 Layout**

Desktop：

```text
45% / 55%
```

左侧 sticky。

右侧环节列表。

---

## **16.2 Left Content**

Eyebrow：

AI 应用

H2：

**AI 可以进入企业哪些环节？**

正文：

不从技术开始，而从企业真正遇到的问题开始。

大型强调：

**先找到问题，** **再决定 AI。**

---

# **17. AI Scenario Rows**

6 个。

顺序：

```text
决策
获客
销售
内容
办公
管理
```

Row 不采用传统大卡片。

采用：

```text
border-bottom
```

的列表式设计。

每个：

```text
padding: 24px 0
```

结构：

```text
01
获客
如何降低获客成本？
相关产品：AI 外呼
 →
```

---

## **17.1 相关产品标注**

每个环节下方标注可对应的产品。

样式：

```text
font-size: 13px
color: #9CA3AF
margin-top: 8px
```

作用：

把抽象环节落到具体可用的工具上，直接回答「这个环节你们有什么东西」。

规则：

- 只标注**确实可对应**的产品
- 没有对应产品的环节，**不显示该行**
- 不得为了填满而强行关联

---

## **17.2 Hover**

Hover 当前项：

- 标题向右移动 4px
- Arrow opacity 0 → 1
- AI Gradient line 出现

不要改变整个列表高度。

---

# **18. Section 04 — AI Implementation**

锚点：`#ai-implementation`

背景：

`#FFFFFF`

---

## **18.1 过渡文案（流程之前）**

标题（H2）：

**一个产品不是终点**

正文：

企业真正需要的，不只是购买一个 AI 工具。

更重要的是找到适合自己的工具，并让它进入真实的工作流程。

从产品体验、团队培训，到具体业务场景的应用，我们希望陪企业把 AI 从「知道」变成「用起来」。

样式：

正文最大宽度 `720px`，与 H2 间距 `24px`。

过渡文案与下方流程标题之间的间距：

```text
72px（Desktop）/ 48px（Mobile）
```

---

## **18.2 流程标题与副标题**

Eyebrow：

服务方式

标题（H2）：

**从一个问题开始**

副标题：

技术只是手段，真正重要的是解决企业的问题。

---

## **18.3 Process**

Desktop：

**五列**。

```text
01      02      03      04      05

了解    判断    选择    尝试    落地
```

每项之间使用极细连接线。

Tablet：

```text
2 列（第 5 项单独占一行）
```

Mobile：

```text
单列纵向
```

---

## **18.4 Content**

### **01**

**了解**

了解企业当前的业务、团队和实际问题。

### **02**

**判断**

判断哪些问题适合使用 AI，哪些问题并不需要 AI。

### **03**

**选择**

在已合作的产品中选择合适的工具，或组合多个工具。

### **04**

**尝试**

在小范围内完成配置、培训与验证。

### **05**

**落地**

验证有效后，再进入实际业务流程并持续优化。

---

## **18.5 服务形式**

流程下方一行轻量说明：

```text
产品体验 / 企业培训 / AI 工作坊 / 场景落地 / 持续优化
```

使用与 Contact 入口标签相同的 tag 样式，居中排列。

不要做成卡片。

---

底部：

**不是为了 AI 而 AI。**

使用较大字号。

---

# **19. Section 05 — Digital Infrastructure**

锚点：`#infrastructure`

背景：

`#F7F8FA`

Desktop：

```text
50% / 50%
```

左：

Eyebrow：

企业数字化

标题（H2）：

**从物理空间，到数字空间**

正文：

我们并不是从 AI 开始。

长期以来，我们一直参与企业办公空间、弱电工程和智慧楼宇相关业务。

今天，我们正在把这种“让企业更智能”的能力，从办公空间进一步延伸到企业经营和工作方式本身。

---

## **19.1 Visual — 递进阶梯（Ladder）**

右侧使用**竖向递进阶梯**，表达「从物理空间到数字空间」的层层推进。

> v2.1 变更：原设计为纯英文标签 `BUILDING / SPACE / DEVICE / DATA / AI`。
> 目标用户是中小企业负责人，英文标签对他们没有沟通价值，已全部改为**中文 + Lucide 图标**。
> 依据 `02_Content.md §13.2 语言规则：中文优先`。

---

### **结构**

每一项固定结构：

```text
① 图标节点（40 × 40，圆角 10）
② 中文标签（17px / 500）
③ 中文补充说明（13px / muted）
④ 编号（13px / muted，右对齐）
```

五项内容：

| 编号 | 标签 | 补充说明 | Lucide 图标 |
|---|---|---|---|
| 01 | 楼宇 | 建筑与园区 | `building` |
| 02 | 空间 | 办公与经营场所 | `grid-2x2` |
| 03 | 设备 | 弱电与终端设备 | `monitor` |
| 04 | 数据 | 采集与联通 | `database` |
| 05 | 智能 | AI 进入实际业务 | `sparkle` |

---

### **样式**

容器：

```text
background: #FFFFFF
border: 1px solid #E5E7EB
border-radius: 12px
padding: 28px（Mobile）/ 40px（≥ 768px）
```

图标节点：

```text
width / height: 40px
border-radius: 10px
border: 1px solid #E5E7EB
background: #F7F8FA
color: #6B7280
icon: 18px
```

节点之间的连接线：

```text
width: 1px
height: 20px
background: #E5E7EB
```

终点（05 智能）强调：

```text
节点 border: rgba(91,124,255,.5)
节点 background: rgba(91,124,255,.08)
图标与标签 color: #5B7CFF
```

强调只允许用在这一个节点上，不做大面积填充。

---

### **要求**

- 使用 `<ol>` + `<li>`，因为它是**有序的递进关系**，语义上就是列表
- 每层只显示一行标签 + 一行补充说明，不得堆叠更多文字
- 这些标签只服务视觉表达，不承担新的业务承诺
- 禁止使用芯片、机器人、电路板等意象（遵守 §3.3）

---

# **20. Section 06 — About**

锚点：`#about`

背景：

`#080A0F`

布局：

```text
40% / 60%
```

左侧：

Eyebrow：

关于义云

H2：

**关于义云科技**

右侧：

正文：

四川义云科技有限公司是一家面向企业客户的数字化服务公司。

公司业务覆盖企业数字化、智能化办公、智慧楼宇及相关技术服务。

我们并不是从 AI 开始。长期以来，我们一直参与企业办公空间、弱电工程和智慧楼宇相关业务。

随着人工智能技术快速进入企业，我们正在把这种「让企业更智能」的能力，从办公空间延伸到企业经营和工作方式本身，并进一步延伸至 AI 产品、AI 应用和落地服务。

我们关注的不只是新技术本身，更关注：

**技术能不能进入真实的企业。**

> 变更：v1 的 `Section 02 — AI Transition` 转型叙事并入此处。
> 因此本段正文比 v1 多一段「我们并不是从 AI 开始」。

---

# **21. Section 07 — Contact**

锚点：`#contact`

背景：

`#FFFFFF`

高度：

Desktop：

```text
500px
```

Mobile：

```text
420px
```

居中布局。

Eyebrow：

先聊问题

H2：

**有一个企业问题，想试试 AI？**

Description：

不需要先想清楚解决方案，从你正在遇到的问题开始。

CTA：

**和我们聊聊 →**

---

## **21.1 Contact Topics**

CTA 上方可以使用三个轻量标签：

```text
AI 产品与选型
企业培训与活动
AI 应用落地
```

不要做成三个大型卡片。

---

# **22. Section 08 — Footer**

背景：

`#080A0F`

颜色：

白 / 灰。

结构：

Desktop：

```text
Brand       业务方向       关于
```

Mobile：

垂直排列。

Brand：

义云科技

YIYUN TECHNOLOGY

让 AI 和数字技术真正进入企业。

业务：

```text
AI 产品与选型
AI 应用落地
企业 AI 培训与活动
智慧楼宇与智能办公
```

关于：

```text
关于义云
联系我们
```

底部：

© 2026 四川义云科技有限公司

备案信息使用真实数据。

---

# **23. CTA Component**

全站只允许三种 CTA 类型。

## **Primary**

深色或 AI Accent。

用于：

**主要转化动作**

---

## **Secondary**

透明背景 + Border。

用于：

**次级动作**

---

## **Text CTA**

文字 + Arrow。

例如：

了解更多 →

用于：

**低优先级导航。**

---

## **External CTA**

v2 新增。

指向第三方产品官网时使用（Product Block 的「了解产品 →」）。

必须：

```text
target="_blank"
rel="noopener noreferrer"
```

必须配合外链图标（arrow-up-right，Lucide），让用户知道将离开本站。

样式：

与 Text CTA 一致，但箭头使用外链图标而非右箭头。

无障碍：

必须包含可供屏幕阅读器识别的提示文本，例如：

```html
<span class="sr-only">（在新标签页打开）</span>
```

---

# **24. Button 规则**

高度：

```text
40px / 44px / 48px
```

根据层级使用。

Radius：

```text
6px      ← v2.2：由 8px 收紧
```

> 圆角档位（v2.2 统一收敛为 CSS 变量，对标 Linear 5–10px / Anthropic 2–4px）：
>
> ```text
> --radius-lg  14px   大容器
> --radius     10px   卡片 / Product Block
> --radius-sm   6px   按钮
> --radius-xs   5px   标签 / 归属标签
> ```

禁止：

- 胶囊形按钮
- 巨大按钮
- 过度圆角
- 多彩按钮

---

# **25. Card 规则**

卡片不是网站主要视觉元素。

只在以下场景使用：

- AI Product Showcase（§15 Product Block）
- 特定 AI 场景

禁止整个网站：

Section → Card → Card → Card → Card

连续堆叠。

页面必须通过：

**Typography + Grid + List + Space**

建立节奏。

---

# **26. Icon**

统一：

**Lucide Icons**

Stroke：

```text
1.5px – 2px
```

颜色继承文本。

禁止：

- Emoji
- 彩色 Icon
- 3D Icon
- 混合 Icon Library

---

## **26.1 图标使用场景**

v2.1 新增。

图标只在**能降低理解成本**的位置使用，不做装饰。

| 位置 | 尺寸 | 用途 |
|---|---|---|
| Product Block 的角色行 | 14px | 让四个产品的角色可被快速区分 |
| Digital Infrastructure 阶梯节点 | 18px | 让「楼宇 → 智能」的递进关系可被快速看懂 |

不使用图标的位置：

- 流程步骤（编号 01–05 已足够清晰，加图标反而增加噪音）
- Hero AI Network（该处是抽象网络，不是图标组合）
- 场景列表（列表式设计已有编号与箭头）

---

## **26.2 图标选择原则**

- 一个图标只表达**一个具体事物**，不表达抽象概念
- 优先选择**实物或常见界面符号**（楼、显示器、数据库、电话、摄影机）
- 不使用需要文化背景才能理解的隐喻
- 不使用芯片、机器人、电路板、地球等意象（遵守 §3.3）

---

## **26.3 与语言规则的关系**

图标不能完全替代文字。

中文标签 + 图标同时使用，而不是「只留图标」。

原因：目标用户是中小企业负责人，纯图标对他们同样存在理解成本。

---

# **27. Motion System**

动画原则：

**Slow / Subtle / Purposeful**

动画不是视觉主角。

---

## **27.1 Reveal**

```text
opacity: 0 → 1
translateY: 20px → 0
duration: 600ms
ease: easeOut
```

Stagger：

```text
80ms
```

---

## **27.2 Card Hover**

```text
translateY(-3px)
duration: 170ms
ease: cubic-bezier(.25,.46,.45,.94)
```

> v2.2：由 `4px / 250ms` 收紧为 `3px / 170ms`。
> Linear 实测 `transition:.16s var(--ease-out-quad)` —— 标杆站的微动效比直觉更快、位移更小。
> 慢而大的位移会显得「廉价」。

**v2.2 补充：不使用 box-shadow。**

层次靠**边框色 + 背景微变化**建立，不靠投影。

```text
hover: border-color: rgba(91,124,255,.45)
       background: #FCFCFE
```

---

## **27.3 Hero Network**

Node：

```text
8–20s
```

Data Flow：

```text
4–8s
```

无限循环。

---

## **27.4 Scroll Trigger**

使用 Intersection Observer 或 Framer Motion viewport。

默认：

```text
once: true
```

元素首次进入 viewport 后执行。

不要反复播放。

---

# **28. Reduced Motion**

必须支持：

```text
@media (prefers-reduced-motion: reduce)
```

此状态：

- 禁止 Node 漂移
- 禁止 Data Flow
- 禁止复杂 Reveal
- 保留必要 hover transition 或直接取消

页面内容必须在无动画情况下完整可用。

---

# **29. Responsive Behavior**

## **Desktop**

Hero：

```text
55 / 45
```

AI Product Showcase：

```text
2 列 + 主推/平台通栏
```

AI Application：

```text
45 / 55
```

Process：

```text
5 columns
```

Infrastructure：

```text
50 / 50
```

About：

```text
40 / 60
```

---

## **Tablet**

Hero：

```text
50 / 50
```

AI Product Showcase：

```text
2 列 + 主推/平台通栏
```

AI Application：

上下布局。

Process：

```text
2 列（第 5 项单独一行）
```

Infrastructure：

上下布局。

About：

上下布局。

---

## **Mobile**

所有复杂 Grid：

```text
1 column
```

Hero：

```text
Content
↓
Visual
```

AI Product Showcase：

```text
1 列
NBBOSS → AI 外呼 → 零一数字人 → 厉害猫 AI
```

Product Block 统一降级为 Standard 尺寸：

```text
padding: 24px
产品名：22px
```

AI Application：

```text
Title
↓
Scenario List
```

Process：

```text
Vertical
```

Infrastructure：

```text
Content
↓
Visual
```

About：

```text
Title
↓
Content
```

---

# **30. Mobile 特殊规则**

移动端不是简单缩小 Desktop。

必须重新调整：

- 内容顺序
- Typography
- spacing
- Visual height
- CTA 宽度

Hero CTA：

两个按钮可以：

```text
inline
```

如果宽度不足：

```text
stack
```

按钮最小点击区域：

```text
44px
```

---

## **30.1 v2.2 移动端细化**

### **Section 间距**

```text
60px 0
```

### **Hero 高度**

```text
min-height: 620px      ← 原 680px
```

### **CTA 宽度**

视口 ≤ 420px 时：

```text
Hero 两个按钮：纵向堆叠 + 各自 100% 宽
Contact 主 CTA：100% 宽，最大 320px
```

原因：320–400px 的机型上并排两个按钮会把文字挤到换行，点击区域也随之变小。

官网的主要访问场景是微信内置浏览器与手机浏览器（§16 原则四），这一档必须照顾。

### **导航当前位置**

桌面端导航启用**当前区块高亮**（scrollspy）：

```text
激活项：文字色 → foreground，底部 1.5px AI Blue 下划线
```

移动端菜单不需要（它是一次性跳转面板，打开即关闭）。

### **实现方式**

用 IntersectionObserver 的 `rootMargin: '-15% 0px -75% 0px'` 建立一条检测带，
而不是监听 scroll 事件 —— 避免滚动抖动与性能损耗。

---

# **31. Accessibility**

必须：

- semantic HTML
- 正确 H1 → H2 → H3 层级
- 所有按钮可键盘访问
- 所有交互元素有 focus state
- 文本与背景满足 WCAG AA
- 装饰性 SVG 设置 aria-hidden
- 图片必须有 alt
- 不通过颜色作为唯一信息

---

# **32. SEO UI 相关规则**

页面只能存在一个：

```text
H1
```

H1：

让 AI 和数字技术真正进入企业

Section 使用：

```text
H2
```

业务卡片使用：

```text
H3
```

不要为了视觉效果破坏语义层级。

---

# **33. 页面状态**

MVP 为静态企业官网。

不需要：

- Loading Screen
- Fake AI Chat
- AI Assistant
- Dashboard
- Login
- User Account

页面加载时直接进入 Hero。

禁止制作：

“正在初始化 AI……”

这种伪 AI Loading。

---

# **34. 404**

需要一个极简 404 页面。

内容：

**页面不存在**

你访问的内容可能已经移动或删除。

按钮：

返回首页 →

视觉与主站一致。

不需要复杂动画。

---

# **35. 空间与视觉节奏**

页面必须避免：

```text
Hero
↓
Card
↓
Card
↓
Card
↓
Card
↓
Footer
```

正确节奏（v2，8 段）：

```text
DARK
Hero

WHITE
AI Product Showcase

DARK
AI Application

WHITE
AI Implementation

LIGHT GRAY
Digital Infrastructure

DARK
About

WHITE
Contact

DARK
Footer
```

整个页面形成：

**Dark → Light → Dark → Light → Light Gray → Dark → Light → Dark**

---

# **36. UI 禁止事项**

AI Coding Agent 不得自行增加：

- 客户 Logo Wall
- 数据统计
- 用户数量
- 成功案例
- Testimonials
- 新闻
- Blog
- Pricing
- 产品商城
- 在线 AI Chat
- AI Copilot
- 复杂 Dashboard
- 虚构产品截图
- 虚构客户照片
- 机器人
- 地球
- 芯片
- 电路板
- 代码雨
- 大面积玻璃拟态
- 过度 3D
- 页面 Loading 动画
- 滚动进度条
- 鼠标跟随光效

除非未来产品需求明确增加。

---

# **37. UI 判断优先级**

如果实现过程中发生冲突，按照以下优先级：

```text
1. 内容可理解性
2. 信息层级
3. 企业可信度
4. 可访问性
5. 响应式体验
6. 页面性能
7. AI 视觉感
8. 装饰性效果
```

**AI 感不能凌驾于可用性和可信度。**

---

# **38. 最终视觉判断标准**

完成后，从一个完全不了解义云科技的企业用户视角检查。

用户打开首页：

**3 秒：**

应该感受到：

现代科技公司 / AI / 企业服务

**10 秒：**

应该理解：

这家公司在做企业数字化和 AI。

**30 秒：**

应该理解：

他们手上有一批可以真实使用的 AI 产品（覆盖决策、获客、内容、企业内部应用），
并且可以帮助企业把这些产品用起来，也做智慧楼宇和智能办公。

**浏览完整页：**

应该形成：

如果我的企业有一个 AI 或数字化问题，可以找他们聊聊。

如果无法达到上述效果，优先调整：

**Typography → Layout → Content hierarchy**

而不是继续添加动画和视觉效果。

---

# **39. UI 完成定义**

UI MVP 完成必须同时满足：

- Desktop 正常显示
- Tablet 正常显示
- Mobile 正常显示
- Hero 信息明确
- 四个 AI 产品与各自角色明确
- **每个产品都有归属标签，且区块有免责说明**
- AI 应用场景明确
- 服务流程明确（5 步）
- 智慧楼宇业务得到保留
- 联系入口明确
- 无虚构业务
- 无虚构数据
- 无虚构案例
- 无不必要功能
- 动画克制
- 页面性能良好
- Accessibility 基础要求满足
- SEO 语义结构正确

最终目标不是：

**做一个很酷的 AI 网站。**

而是：

**做一个看起来足够现代、可信，同时能够容纳义云科技未来 AI 业务发展的企业官网。**