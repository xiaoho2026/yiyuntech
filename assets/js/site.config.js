/* ==========================================================================
 * 站点配置 —— 全站唯一需要修改的文件
 *
 * 修改后刷新页面即可生效，无需构建、无需重新部署流程。
 * 字段留空 = 不渲染该元素（严格遵守 02_Content.md：不显示虚假联系方式）。
 *
 * 留空字段会在页面上以橙色虚线框 + 「待配置」标记提示，填好后自动消失。
 * ========================================================================== */
window.SITE_CONFIG = {
  // 主域名，用于 canonical / og:url。例：'https://www.example.com'
  domain: '',

  // ICP 备案号。例：'蜀ICP备12345678号-1'
  icp: '',

  // 联系方式。全部留空时，CTA 不指向任何地址并标记「待配置」
  contact: {
    phone: '',           // 例：'028-8888 8888'
    email: '',           // 例：'hello@example.com'
    wechat: '',          // 微信号
    address: '四川 · 成都' // 已确认信息，可按需细化
  }
};
